import 'package:flutter/material.dart';
import 'app.dart';

/// ===========================================
/// MAIN ENTRY POINT
/// ===========================================
/// This is where the app starts.
/// We keep it minimal - just runs the app.
/// All configuration is in app.dart
/// ===========================================

void main() {
  runApp(const BooklyApp());
}